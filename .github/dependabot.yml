version: 2
updates:
  # Keep the pinned GitHub Actions SHAs current. The Go module has no
  # third-party dependencies, so there is no gomod ecosystem to track.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
